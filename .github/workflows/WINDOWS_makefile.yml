name: WINDOWS

on:
  push:
    branches: [ "main" ]
  pull_request:
    branches: [ "main" ]

jobs:
  build:

    runs-on: windows-latest

    steps:
    - uses: actions/checkout@v3

    - name: Build
      run: make

    - name: Build with Clang
      run: make CPPC=clang++ Final_Name=CLI_Timer_clang